apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: trino-catalog-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: nks-block-storage
  